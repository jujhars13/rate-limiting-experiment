# Simple Rate Limiting using Envoy + Redis

A simple bun.js web app that's fronted by [Envoy proxy](https://www.envoyproxy.io/) A  leverages Redis as a rate limiting state store.

## Needs
  - docker compose
